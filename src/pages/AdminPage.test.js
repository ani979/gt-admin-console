import { render, screen } from "@testing-library/react";

describe("renders Admin Page", () => {
  it("contain Search bar", () => {});

  it("contains table", () => {});

  it("contains pagination row", () => {});
});
